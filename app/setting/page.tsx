'use client'

import SettingsContainer from "@/components/profile-settings/SettingsContainer"

const SettingPage = () => {
    return (
        <main>
            <SettingsContainer />
        </main>
    )
}

export default SettingPage